module Routing exposing (..)

import Browser exposing (UrlRequest)
import State exposing (Message)
import Url exposing (Url)


onUrlChange : Url -> Message
onUrlChange _ =
    Debug.todo "TODO"


onUrlRequest : UrlRequest -> Message
onUrlRequest _ =
    Debug.todo "TODO"
